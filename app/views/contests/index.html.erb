<p id="notice"><%= notice %></p>

<div class="container riders">
     <h1 class="text-center">Contests</h1>
      <p><strong>we have now <%=@contests.length %> Contests records :</strong></p>
  <div class ="row-padding" id="demo">

<table class="table table-bordered table-hover table-condensed ">
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Email</th>
      <th>Slogan</th>
      

    </tr>
  </thead>

  <tbody>
    <% @contests.each do |contest| %>
      <tr>
        <td><%= contest.first_name %></td>
        <td><%= contest.last_name %></td>
        <td><%= contest.email %></td>
        <td><%= contest.slogan %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contest', new_contest_path %>
</div>
</div>
<div class="container"
<h3 class="card-text">Rocky Mountains,Colorado</h2>
<p>First April  2020</p>
<h6 class="card-title" >Starts in:</h6>
<%= render ("layouts/counter") %>
</div>
